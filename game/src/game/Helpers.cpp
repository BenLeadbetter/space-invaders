#include <game/Helpers.h>

namespace space_invaders::game {

Object makePlayer() {
  return {
      .raster = {},
      .offset = {},
  };
}

Object makeBullet() {
  return {
      .raster = {},
      .offset = {},
  };
}

}  // namespace space_invaders::game
